<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Editar Reserva | Restaurante Premium</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #6C5CE7;
      --light: #F5F6FA;
      --dark: #2D3436;
    }
    
    body {
      font-family: 'Montserrat', sans-serif;
      background: var(--light);
      color: var(--dark);
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .edit-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 500px;
      padding: 2rem;
      margin: 2rem;
    }

    h1 {
      color: var(--primary);
      text-align: center;
      margin-bottom: 2rem;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
    }

    input, select {
      width: 100%;
      padding: 0.8rem;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-family: 'Montserrat', sans-serif;
    }

    .btn-group {
      display: flex;
      gap: 1rem;
      margin-top: 2rem;
    }

    .btn {
      flex: 1;
      padding: 0.8rem;
      border-radius: 8px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s;
      text-align: center;
      text-decoration: none;
    }

    .btn-primary {
      background: var(--primary);
      color: white;
      border: none;
    }

    .btn-outline {
      background: white;
      color: var(--primary);
      border: 1px solid var(--primary);
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="edit-container">
    <h1>Editar Reserva da Mesa <%= reserva.numeroMesa %></h1>
    
    <form method="post" action="/reservas/<%= reserva._id %>/editar">
      <div class="form-group">
        <label for="nomeCliente">Nome do Cliente</label>
        <input type="text" id="nomeCliente" name="nomeCliente" value="<%= reserva.nomeCliente %>" required>
      </div>
      
      <div class="form-group">
        <label for="numeroMesa">Número da Mesa</label>
        <input type="number" id="numeroMesa" name="numeroMesa" value="<%= reserva.numeroMesa %>" required>
      </div>
      
      <div class="form-group">
        <label for="dataHoraReserva">Data e Hora</label>
        <input type="datetime-local" id="dataHoraReserva" name="dataHoraReserva" 
               value="<%= reserva.dataHoraReserva.toISOString().slice(0,16) %>" required>
      </div>
      
      <div class="form-group">
        <label for="status">Status</label>
        <select id="status" name="status">
          <option value="reservado" <%= reserva.status === 'reservado' ? 'selected' : '' %>>Reservado</option>
          <option value="ocupado" <%= reserva.status === 'ocupado' ? 'selected' : '' %>>Ocupado</option>
          <option value="disponível" <%= reserva.status === 'disponível' ? 'selected' : '' %>>Disponível</option>
        </select>
      </div>
      
      <div class="form-group">
        <label for="contatoCliente">Contato</label>
        <input type="text" id="contatoCliente" name="contatoCliente" value="<%= reserva.contatoCliente %>" required>
      </div>
      
      <div class="btn-group">
        <button type="submit" class="btn btn-primary">Salvar Alterações</button>
        <a href="/" class="btn btn-outline">Cancelar</a>
      </div>
    </form>
  </div>
</body>
</html>